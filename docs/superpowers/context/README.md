# Context recaps

Cross-session context documents for long-running initiatives (multiple specs/plans).
A recap travels with the repo (unlike machine-local memory): when resuming an initiative
on any machine/session, reference the recap file to reload context.

Format: `<date>-<initiative>-recap.md` — TL;DR, original ask, decisions so far,
workstream graph, current status, next steps.
